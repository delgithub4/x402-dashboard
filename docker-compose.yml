version: "3.9"

services:

  x402-dashboard:

    build: .

    container_name: x402-dashboard-service

    restart: unless-stopped

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - .:/app
